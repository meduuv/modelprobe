# modelprobe

Read-only helpers for inspecting declared model capabilities.

```python
from modelprobe import capabilities
print(capabilities({"capabilities": ["chat", "tools"]}))
```

Run `pytest` to test locally.

License: MIT. Built by meduuv. `guns.lol/meduu`
