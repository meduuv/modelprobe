[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "modelprobe"
version = "0.1.0"
description = "Read-only model capability inspection"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src/modelprobe"]
